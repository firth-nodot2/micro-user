spring:
  application:
    name: micro-user
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:user
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8082

